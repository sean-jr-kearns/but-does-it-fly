[package]
name = "sender"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol" }

[lints.clippy]
all = "deny"
pedantic = "deny"
nursery = "deny"
panic_used = "deny"
unwrap_used = "deny"
todo = "deny"
unimplemented = "deny"

[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
